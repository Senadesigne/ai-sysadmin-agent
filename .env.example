# Copy to .env (do not commit .env)
# Windows PowerShell:  Copy-Item .env.example .env
# macOS/Linux:         cp .env.example .env

# Where local state is stored (default: repo-root/.data)
DATA_DIR=.data

# REQUIRED in production (auth). Generate your own:
# python -c "import secrets; print(secrets.token_urlsafe(32))"
CHAINLIT_AUTH_SECRET=<GENERATE_ME>

# Optional keys (app should not crash if missing)
GOOGLE_API_KEY=

# Optional toggles (may be wired later)
DEBUG=false
RAG_ENABLED=false

# Copy to .env (do not commit .env)
# Windows PowerShell:  Copy-Item .env.example .env
# macOS/Linux:         cp .env.example .env

# Where local state is stored (default: repo-root/.data)
DATA_DIR=.data

# REQUIRED in production (auth). Generate your own:
# python -c "import secrets; print(secrets.token_urlsafe(32))"
CHAINLIT_AUTH_SECRET=<GENERATE_ME>

# Auth mode: dev or prod
AUTH_MODE=dev
# In dev: allow bypassing login prompt
DEV_NO_AUTH=true

# Admin login (identifier is not secret)
ADMIN_IDENTIFIER=admin
# Admin password (secret) - set this in your real .env
ADMIN_PASSWORD=

# LLM Configuration (optional - app will work without it)
# Get your API key from: https://aistudio.google.com/app/apikey
# If not set, the app will show a message asking users to configure it
GOOGLE_API_KEY=

# SSH key for remote connections (optional)
SSH_KEY_PATH=

# Optional toggles (may be wired later)
DEBUG=false
RAG_ENABLED=false
